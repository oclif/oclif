language: node_js
node_js:
  - node

cache: <%- yarn ? 'yarn' : 'npm' %>

script:
<%_ if (type === 'single' || type === 'multi') { _%>
  - ./bin/run --version
  - ./bin/run --help
<%_ } else if (type === 'plugin') { _%>
  - ./bin/run --help
<%_ } _%>
  - <%- yarn ? 'yarn' : 'npm' %> run test
